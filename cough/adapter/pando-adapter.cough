{ connectTo }   = require '../bridge'
{ connectPort } = require './port-registrar'
{ reactIntake } = require '../aspen-state'

connectViewToController = ->
  connectTo reactIntakePort

reactIntakePort = connectPort reactIntake

module.exports = connectViewToController
