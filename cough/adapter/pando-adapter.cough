{ connectTo }   = require './react-bridge.js'
{ connectPort } = require './port-registrar.js'
reactIntake     = require './react-intake.js'

reactIntakeBus = connectPort reactIntake

connectViewToController = -> connectTo reactIntakeBus

module.exports = connectViewToController
