{ appStateProperty }               = require '../aspen-state'
{ connect, interpret, plug, push } = require './channel-connectors'
{ deleteDispatcher, getDispatcher,
  getEventStream, getProperty }    = require './channel-registrar'

module.exports = {
  appStateProperty
  connect
  deleteDispatcher
  getDispatcher
  getEventStream
  getProperty
  interpret
  plug
  push
}
