{ connect, interpret, plug, push } = require './channel-connectors'

{ getDispatcher, getEventStream, getProperty } =
  require './channel-registrar'

module.exports = {
  connect
  getDispatcher
  getEventStream
  getProperty
  interpret
  plug
  push
}
